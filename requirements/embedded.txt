# Embedded requirements.

# Those dependencies are currently embedded in the repository
# in ./vendor-local/lib/python/. There is no need to install them.

elasticsearch==1.0.0
feedparser==5.1
http://ftp.tummy.com/pub/python-memcached/old-releases/python-memcached-1.47.tar.gz
peep==2.0
pytz==2012d
raven==3.1.16
South==0.8.1
urllib3==1.8
